# TASK-20260918 — specification implementation

Source: docs/dev/开发提示词_20260918.md. All feature groups are implemented. ADR-0005 records the storage and editor decisions.

| Requirement group | Implementation |
| --- | --- |
| Memory files | AGENTS.md owns conventions and current architecture; CLAUDE.md references it |
| Titlebar / settings | Theme-aware custom titlebar, native controls, transparent window + opacity, removed config migration/recent-files data pane |
| Editor menus / tables | Categorized hover menus, numeric picker, range-aware actions/copy, row/column drag, keyboard insertion |
| Document views / export | Ellipsis dropdown, wide/reading/presentation, PDF system print, DOCX export, copy path; status path has no double-click handler |
| Diagrams | Ctrl-wheel zoom, AI source-line reference, editing render suspension, resizable immersive split view, sidebar restoration |
| Help / attachments / code | Grouped searchable help, named relative attachments with native boundary validation, non-mutating CSS range highlights |
| Palette | Focus transfer, input-first layout/clear, footer regex, right-aligned shortcuts, live theme selection with cancellation |
| Persistence | SQLite metadata/credentials/history/workspaces, executable-sibling .yeditor, managed res/type files, no legacy disk import |
| Provider / resources | Named provider tables + automatic models, AI submenus, copied SKILL files, tagged named prompts, per-workspace enabled flags |
| AI interactions | Models, sessions/search/fork, dedicated resource dialog, reasoning, @ and / suggestions + compact/new, safe streaming Markdown bubbles, usage/speed/errors, full editor mode, reference chips, images |

Verification on macOS ARM64:
- pnpm lint / typecheck / test / build.
- 49 frontend tests; 17 Rust tests; cargo fmt and clippy with warnings denied.
- 8 bundled Pi contract tests, including image + reasoning stream, tool bridge, provider errors and cancellation.
- Explicit private-provider E2E through bundled Pi: text response and usage received successfully. No credential printed or stored in source/build fixtures.
- Browser smoke: settings/resources, document menu, palette focus/theme mode, shortcut help, diagram references and immersive editing, code edit/Markdown round-trip.
- macOS application bundle built locally. Native OS dialogs/window transparency and Windows UI are not certified by browser testing; native UI automation was unavailable while the host was locked.

The private source document is ignored by version control. Portable packaging filters .yeditor and preserves existing expanded application folders. Never copy the private provider credential into task records, fixtures, logs or release artifacts.

Final artifact: artifacts/y-editor-0.1.0-dev.1-macos-arm64-portable.zip (41.7 MB), with SHA-256 sidecar. macOS ad-hoc signature verification passed. Archive inspection verified no .yeditor database, private specification or private test credential was included. The archive is a direct-distribution development build, not notarized.
