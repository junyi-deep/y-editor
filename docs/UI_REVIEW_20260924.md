# v0.0.4 UI and interaction validation

The independent impeccable finish review returned **ship, provided required checks pass** after one correction batch. All three findings were resolved: modified Enter no longer accepts composer suggestions, editor context-menu hints resolve effective/platform shortcuts, and the narrow screenshot was replaced with an actual 810×741 capture. No visual rebuild was requested. The changed-target detector reported no findings.

Browser evidence in the local `.impeccable/review/` folder covers light/dark desktop editor and settings, the separate history dialog, the seven-button table toolbar, and the narrow composer. At 810×741, the suggestion list bottom was 527px and composer top 534.5px. Shift+Enter produced a newline. An outside click into the composer dismissed the document menu. Search focus had no box shadow or visible outline. History and settings dismissed with Escape. Mermaid preview zoom resized the inline SVG while the stage transform contained translation only.

Automated coverage includes approval defaults, active-document automatic approval, conflict preservation, bounded backend rejection, visible and actionable proposals, separate session dialog dismissal, IME Enter, Shift+Enter, image references, row/column reordering and undo, table deletion and undo, right-click rectangular selection, precise source/rich-text ranges for repeated text, vector zoom and shortcut hints. The real Pi sidecar is exercised against local provider fixtures.

Limits: browser-only previews intentionally show desktop IPC errors. These captures do not certify successful private-provider execution, native multi-window flows, OS dialogs, Windows runtime, or WebKit raster quality. Those paths must not be described as visually verified. Production builds retain the existing editor/Mermaid large-chunk warning.

Final local checks passed on 2026-09-24: `pnpm lint`, `pnpm typecheck`, 86 frontend tests in 16 files, `pnpm build`, Rust fmt check, Clippy with warnings denied, 19 Rust tests, and 8 Pi sidecar fixture integration tests. No private provider credential was used.
